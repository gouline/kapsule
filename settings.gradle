rootProject.name = 'kapsule'

include 'kapsule-core'

if (false && !hasProperty('excludeSamples')) {
    println('Including sample projects')
    include 'samples:simple', 'samples:android'
} else {
    println('Excluding sample projects')
}
